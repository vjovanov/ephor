//! What binds and runs (§AR-001-layers.1).
//!
//! A seam is where a capacity ephor does not embody is reached, and every one
//! of them has the same anatomy: a contract in materials, a configured binding,
//! a shipped default, and a stated degrade (§REQ-001-boundary.1). This layer
//! holds the machinery all of them share — the executor that runs a summons
//! (§AR-002-summons) and the reader that validates what comes back
//! (§FS-006-project-interface.4).

pub mod answer;
pub mod checks;
pub mod dossier;
pub mod gate;
pub mod summons;
